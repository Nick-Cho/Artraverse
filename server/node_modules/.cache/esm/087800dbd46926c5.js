let expressJwt,Post;_d46‍.x([["requireSignIn",()=>requireSignIn],["canEditPost",()=>canEditPost]]);_d46‍.w("express-jwt",[["default",["expressJwt"],function(v){expressJwt=v}]]);_d46‍.w("../models/post",[["default",["Post"],function(v){Post=v}]]);


       const requireSignIn = expressJwt({
  secret: process.env.JWT_SECRET,
  algorithms: ['HS256']
});

       const canEditPost = async (req,res, next) =>{
  //next is a callback function
  try{
    const post = await Post.findById(req.params._id);
    _d46‍.g.console.log("post received from canEditPost middleware: ", post);
    if (req.user._id != post.postedBy){
      return res.status(400).send("Unauthorized");
    }
    else{
      next();
    }
  } catch(err){
    _d46‍.g.console.log(err);
  }
}